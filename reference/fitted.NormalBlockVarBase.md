# Extracts fitted values from objects returned by \[NormalBlockVarBase()\] and its variants

Extract fitted values from an NormalBlockVarBase object.

## Usage

``` r
# S3 method for class 'NormalBlockVarBase'
fitted(object, ...)
```

## Arguments

- object:

  An object of class NormalBlockVarBase.

- ...:

  not used, only here for S3 compatibility

## Value

A matrix of fitted values extracted from the object.
