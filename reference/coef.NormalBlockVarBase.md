# Extracts model coefficients from objects returned by \[NormalBlockVarBase()\] and its variants

Extract coefficients from an NormalBlockVarBase object.

## Usage

``` r
# S3 method for class 'NormalBlockVarBase'
coef(object, ...)
```

## Arguments

- object:

  An object of class NormalBlockVarBase.

- ...:

  not used, only here for S3 compatibility

## Value

A matrix of coefficients extracted from the NormalBlockVarBase model.
